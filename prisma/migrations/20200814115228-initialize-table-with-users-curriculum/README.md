# Migration `20200814115228-initialize-table-with-users-curriculum`

This migration has been generated by Expedito Andrade at 8/14/2020, 8:52:28 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"id" SERIAL,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"email" text  NOT NULL ,
"firstName" text   ,
"lastName" text   ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Curriculum" (
"id" SERIAL,
"aboutMe" text  NOT NULL ,
"candidateId" integer  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Profile" (
"bio" text   ,
"address" text   ,
"contactNumber" text   ,
"userId" integer  NOT NULL )

CREATE UNIQUE INDEX "User.email_unique" ON "public"."User"("email")

CREATE UNIQUE INDEX "Curriculum_candidateId" ON "public"."Curriculum"("candidateId")

CREATE UNIQUE INDEX "Profile.userId_unique" ON "public"."Profile"("userId")

ALTER TABLE "public"."Curriculum" ADD FOREIGN KEY ("candidateId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Profile" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200814115228-initialize-table-with-users-curriculum
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,37 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id Int @id @default(autoincrement())
+  createdAt DateTime @default(now())
+  email String @unique
+  firstName String?
+  lastName String?
+  curriculum Curriculum?
+  profile Profile?
+
+}
+
+model Curriculum {
+  id Int @id @default(autoincrement())
+  aboutMe String
+  candidate User @relation(fields: [candidateId], references: [id])
+  candidateId Int
+}
+
+model Profile {
+  bio String?
+  address String?
+  contactNumber String?
+  user User @relation(fields: [userId], references: [id])
+  userId Int @unique
+}
```


