type Query {
  allCurriculums: [Curriculum]
  users: [User]
}

type Curriculum {
  candidate: User
  aboutMe: String
}

type User {
  id: ID!
  firstName: String
  lastName: String
  email: String
}

